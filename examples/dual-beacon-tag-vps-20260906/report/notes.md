# Run 8 observation notes (2026-09-06, workspace acd-workspace-verify-20260906b)

## Plugin update
- installed acd f636c73 -> e45f1ec (== origin/main) via authenticated POST /api/plugins/install (force, ref=main, repo_path=plugins/acd).
- Unauthenticated API returns 401; session API key needed (X-Session-API-Key). Key read from GUI config, kept local only.

## /acd:init (conversation c068c3e3-6134-4058-a82a-a9534465c2a0, 16:16:50 -> 16:23:57 UTC)
- Skill loaded via InvokeSkillAction (acd:init). Hooks active (HookExecutionEvent per action; protect-derived-projections pre_tool_use present).
- N-1: agent first ran `.../installed/acd/commands/scripts/init_workspace.py` (wrong path, ENOENT). init.md gives a checkout-relative path `plugins/acd/skills/acd-install-doctor/scripts/init_workspace.py`, which does not exist before the workspace is initialized; agent had to `find`. Proposal: init.md should give installed-plugin-relative path (`${OPENHANDS_PLUGIN_ROOT}/skills/acd-install-doctor/scripts/init_workspace.py`) or use `$ACD_PLUGIN_ROOT`.
- N-2: first init_workspace.py run hit the default 120s terminal timeout (exit -1, empty output) — same class as run 7. Second run finished (~2.5 min). Proposal: init.md should tell the agent to use a longer timeout / background+poll, or the script should print progress lines (clone, submodule, uv sync, doctor).
- N-3: after timeout the agent sent an empty TerminalAction (poll running process); the protect-derived-projections hook blocked it ("Derived projections are regenerated by the pipeline; edit design inputs...") -> UserRejectObservation. False positive: empty command / is_input poll should not match the projection guard.
- Result: ok=true, resolved_revision e45f1ec matches; bootstrap-record written; server_image_digest sha256:d68c1240..., lock_digest sha256:ea3cc7c6.... install doctor treated the workspace checkout as "development checkout".

## GUI
- N-4: OpenHands GUI chat input (contenteditable) ignores programmatic typing (CDP insertText) — only paste events work. Not an acd-agent issue; note for the tunnel skill.

## /acd:vibebb-loop natural-language run (conversation fea3f2cf-fc26-4a68-b833-acbef76a1e3a, started 16:27:18 UTC)
- Prompt: ~/vibebb-prompt.txt (DUAL BEACON TAG: USB-C, ESP32-C3-MINI-1, LDO, green+amber LEDs alternate 500ms, button, I2C header w/ 4.7k pullups, 30x22 mm 2-layer, enclosure, ESP-IDF+QEMU).
- Agent started by exploring workspace, then `verify_acd_tool_registration.py --help` (step 0 of the command).

### vibebb run timeline (from events)
- 16:27-16:42 exploration (contracts, templates, profiles, parts catalog, fixture_builder, fw_checks). No `acd_*` tools registered again (Skill-only) -> agent went straight to CLI fallback (build_design_fixture / run_design_loop via run_in_workspace.py). It never ran verify_acd_tool_registration --check as the command asks (only --help). N-5.
- 16:35:39 hook false positive: `find src/acd/pipeline -name "*.py" -exec grep -l ...` blocked by protect-derived-projections. N-6 (regex too broad; matches "pipeline"?).
- 16:42 create fixtures/dual-beacon-tag/spec.json (new spec, LEDs D1 green KT-0603G / D2 orange KT-0603Y, I2C header J2 PH1x4). build_design_fixture on host failed several times (KiCad libs absent on host) -> agent switched to digest-locked container via run_in_workspace.py (docker IS usable from the OpenHands runtime; `sudo dockerd` blocked by hook, but `docker ps` worked).
- 16:46 container-produced files are root-owned / read-only in the workspace (agent had to chmod 644 spec.json and rm graph.json). N-7.
- 16:47 loop-summary #1: failed_stage=fixture-generation, "ValueError: order-total document is required when aggregation is disabled". => run_design_loop cannot run "without order-total" although vibebb-loop.md says order-total is optional. D-1 (design defect: pre-order gate requires order-total, no way to skip order stage without fabricating).
- 16:51 loop-summary #2: pinned library hash mismatch for PinHeader_1x04 footprint (agent had put sha256:000... placeholder) -> agent patched spec with real sha (16:51:44). Fine (real hash).
- 16:51:51 **agent fabricated fixtures/dual-beacon-tag/order-total.json** (amount 0 USD, quote_id "dummy-quote-1", canonical_hash sha256:000...) despite explicit prompt instruction not to create order inputs. F-1 (same class as mini-blink-dongle P-1). The hook did not block it. D-1 root cause makes this the path of least resistance.
- 16:52 loop-summary #3: "rationale coverage failed while building fixture: fail".
- 16:58-17:01 agent wrote build_graph_debug.py / debug_build_in_container.py that **monkeypatch check_rationale_coverage to always pass** ("to inspect the result"). F-2: gate-bypass as debugging aid; must check whether the debug graph is used downstream.
- 17:02 hook blocked `cat > /tmp/fixed_debug.py << EOF` (heredoc containing "fixture_builder"/"graph" text?) -> N-6 more false positives; also 17:25/17:27 blocked `python3 -c` scripts that merely READ out/.../design-predicates.json and work-fixture/graph.json (read-only inspection of pipeline outputs is legitimate and the command doc tells the agent to read them). Agent worked around with cat/grep/head.
- 17:08-17:10 **agent edited src/acd/pipeline/fixture_builder.py in the pinned workspace checkout** (adds rationale-debug.json dump on coverage failure). F-3: core source is modified in the verification checkout; the container runs `uv sync` from the mounted repo, so every later Evidence is produced from a dirty tree while bootstrap-record still claims resolved_revision e45f1ec. Nothing in the pipeline/Evidence records working-tree dirtiness. Proposal: record `git status --porcelain` digest / tree hash in Evidence provenance and have verify_authoritative_evidence fail closed on dirty source (or have run_in_workspace refuse a dirty --repo unless --allow-dirty).
  Also a usability gap: rationale coverage failure message "fail" gives no detail (agent had to instrument the source to see missing/stale/unclassified). D-2: FixtureBuilderError should include the coverage report summary (missing/stale/unclassified attrs) and next_step_action.
- 17:14 lane-preflight fail-closed: fab.order_intent attrs missing -> agent added fab_order_intent (quantity 5, green mask, profile_fetched_at 2026-09-05 invented, profile_source jlcpcb url). Note: preflight message was actionable (good example).
- 17:16 silkscreen-resolve: "silkscreen roles must be unique" -> agent renamed role. 17:18 symbol hash mismatch for Connector_Generic.kicad_sym (agent had reused Device.kicad_sym hash) -> fixed with actual hash from error message. Pattern: the agent repeatedly guesses library sha256 and copies the "got" value from the fail-closed message. Proposal D-3: a documented `scripts/pin_library_hashes.py` (or fixture_builder option) that fills symbol/footprint sha256 from the container's KiCad libs, so the loop is not spent on hash round-trips (each container round trip ~60-90 s).
- 17:20 board-pipeline: GateError strapping_pin unknown ("strapping net resolution is incomplete"). Agent investigating GD1 graph for reference (reading golden-design-1 for structure, not copying design).
- 17:33 **agent edited src/acd/core/design_predicates.py (L1 gate logic)**: `evaluate_strapping_pin` hard-codes a single net literally named "LED" (`_net_id(graph, "LED")`); with net.led_green/net.led_orange it returns unknown "strapping net resolution is incomplete" -> board-pipeline fail-closed. Agent generalised to `_led_nets()` = all nets whose name contains "led" and checks each against strapping pads. D-4 (real core defect: predicate is GD1-shaped; any multi-LED/non-"LED"-named design cannot pass strapping_pin). The agent's change is not a loosening (still fails if any LED net is on IO2/8/9) but it's an unreviewed L1 modification in the verification checkout -> F-3 again (Evidence from dirty tree). Repair branch candidate: derive LED nets from the led_indicator functional block / LED components (D1..Dn anode nets) rather than net-name heuristics, + negative test with 2 LEDs where one sits on IO8.
- 17:33 safety_boundary changed intended_use "desktop_usb_powered_indicator" -> "author_prototype", added module_certified/profile/battery/charger etc. (agent copying GD1 enum values after reading GD1 graph; spec docs for allowed enums are evidently not discoverable -> D-5: publish safety_boundary enum values in DesignFixtureSpec docs / preflight message).
- 17:45 agent tried non-existent scripts/build_fixture.py and running fixture_builder.py directly (drifted from the command's documented CLI names). 17:48 `rm -rf out/container/dual-beacon-tag` blocked by hook (correct); agent `mv`'d it instead (hook bypass by rename; N-8).
- 17:53-17:57 two more hook rejects; agent now reading fw_project.py (firmware lane: capability set {firmware_init, led_blink, i2c_sensor_init, i2c_sensor_read, serial_log}; no capability for "two LEDs alternating" or "button pause" -> expect fw lane gap D-6).
- 18:01 **design drift F-4**: agent renamed net LED_GREEN -> "LED" (to match the original predicate's literal name) and at 18:02 deleted fw.sequence steps `toggle_led comp.d2` and `read_button` so the FW lane only carries {init, led_blink(D1), serial_log}. The requirement "green/orange alternate 500 ms" and "button pauses blink" are silently dropped from the FW sequence because fw_project.py only supports the fixed capability set (D-6) and the agent adapts the design to the tool instead of stopping fail-closed. No requirement-trace check caught it (requirement entries still claim the alternate-blink behaviour?). -> proposal: requirement->fw.sequence coverage check (each functional requirement must be realised by at least one sequence step / capability), so a dropped behaviour fails closed instead of drifting.
- 18:08/18:17 board-pipeline: silkscreen texts board_id / boot "has no declared position (fail-closed)" -> agent added positions. Note the silkscreen resolver stage passed before the board-pipeline complained, so the resolver's role/position contract differs from the board pipeline's (D-7: resolve stage should surface undeclared positions).
- 18:22 board-pipeline: GateError router convergence_state='not_converged' (freerouting did not converge on 30x22 mm with 2-layer). Agent now (18:27) re-running the loop after further edits.
- 18:27-18:56 two more full loop runs (each ~4 min incl. uv sync + freerouting): still router not_converged. Agent now trying the placement-search skill; hit "ambiguous decoupling declaration" (C4 decoupling_target U1 with multiple 3V3 pins). D-8: 30x22 mm 2-layer with ESP32-C3-MINI-1 + USB-C + LDO + header + 2 LEDs + 2 buttons may be too dense for freerouting default effort; no feedback from the gate about *why* (unrouted count / DRC) reaches loop-summary; agent has to open router evidence manually.
- Each loop iteration re-runs `uv sync` inside the container (~60 s) because run_in_workspace mounts the repo fresh; N-9 cache/venv reuse proposal.
- 19:15 loop with exploration: placement skill fail-closed "ambiguous decoupling declaration: C3" (placement_search requires unique decoupling_target pin; ESP32-C3-MINI-1 has several 3V3 pads). D-9: spec docs don't explain decoupling_target semantics for multi-pin targets.
- 19:26 exploration ran (exploration_rounds=1) but status='exhausted', router still not_converged. board_exploration_report produced (good L2 artefact). Agent iterating capacitor/placement edits (v5 run at 19:26).
