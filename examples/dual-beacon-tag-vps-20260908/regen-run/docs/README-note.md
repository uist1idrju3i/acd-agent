# 収録注記

`product-readme.md`の図・音源リンクはcontainer出力時の`../../out/dual-beacon-tag/dual-beacon-tag/`相対パスを、収録先の`../visual/`・`../theme-song/`へ書き換えている（`fixed-run/docs/`と同じ扱い）。本文は無改変。

`../loop/agent-final-report.md`はagent報告全文で、収録時にMarkdown見出し階層のみ1段繰り上げた（`##`→`#`、`###`→`##`、`####`→`###`）。本文は無改変。
