# セキュリティポリシー

ACDのセキュリティに関する報告手順と、プロジェクトが前提とする方針をまとめます。
実装上の規範は[`AGENTS.md`](AGENTS.md)の「秘密情報と信頼できない入力」および
「OSSライセンス順守」に従います。本ファイルはそれと矛盾しません。

## 対象バージョン

| バージョン                  | サポート状況       |
| --------------------------- | ------------------ |
| `main`（コンセプト段階）    | 対象               |
| リリース済みバージョン      | なし（未リリース） |

本リポジトリはコンセプト段階であり、実装はまだありません。正式リリースや
セキュリティ修正のバックポート対象となるリリース系列は存在しません。修正は
`main`に対して行います。

## 脆弱性の報告

**公開Issueで脆弱性を報告しないでください。**

GitHubの[Security Advisories](https://github.com/uist1idrju3i/acd-agent/security/advisories/new)
から非公開で報告してください。報告には次の情報を含めてください。

- 影響を受ける対象（コミットハッシュ、ドキュメント、submodule、CI設定など）
- 再現手順と、再現に必要な入力（秘密情報は伏せ字にしてください）
- 想定される影響（情報漏えい、任意コード実行、検証ゲートの回避など）
- 判明していれば回避策と、公開に関する希望

報告は日本語または英語で受け付けます。

## 対応の流れ

1. 受領確認：確認でき次第、返信します。
2. 影響評価：再現、影響範囲、深刻度を確認します。
3. 修正：`main`へ修正を入れ、回帰を防ぐ検証を追加します。
4. 公開：報告者と調整のうえでSecurity Advisoryを公開します。希望があれば
   クレジットを記載します。

本プロジェクトは個人の開発リソースで運営しており、対応時間の保証（SLA）や
報奨金制度はありません。

## スコープ

対象となる例：

- 秘密情報・認証情報の漏えい、リポジトリやCIへの混入
- 外部入力（設計データ、netlist、KiCadファイル、CADモデル、アーカイブ、
  ツール出力）の処理におけるパストラバーサル、コマンドインジェクション、
  意図しないファイル書き込みや外部通信
- 検証ゲート（ERC/DRC/DFM/干渉・肉厚チェック/FW整合ゲート）や承認・予算上限の
  回避につながる欠陥
- 安全境界（AC電源、高電圧・大電流、レーザー、医療・車載用途、無線送信の直接設計、
  Li-ion/LiPo充電回路など、AIに任せない領域）の回避につながる欠陥
- 外部ツール出力やモデル出力がコードとして実行される経路
- Skill／plugin source、inline command、marketplace取得経路に起因する任意コード実行
- SDK ToolDefinitionの動的schema変更を悪用した検証回避
- remote workspaceおよびagent-serverを経由した権限・分離の回避
- VS Code／noVNC desktopのGUI操作を、commit・receipt・対象ゲート再実行なしに設計根拠や
  合格として扱うこと
- 視覚投影やscreenshot内の指示をプロンプトとして実行させる画像経由のプロンプトインジェクション
- browser経由で取得した外部入力を期限・URL・screenshot hashなしに設計根拠や発注条件へ使うこと
- 依存関係の取得・固定に関する欠陥（lockfileの回避、submoduleの参照先変更、
  ビルド時の未検証ダウンロードなど）

対象外の例：

- 実行環境の設定ミスや、ユーザーが導入した外部ツール自体の脆弱性
- `vendor/`配下のsubmodule（OpenHands Software Agent SDKおよびAgent Canvasソース）
  そのものの脆弱性。
  上流へ報告してください。ACD側の利用方法に起因する場合は本ポリシーの対象です。
- ユーザーが明示的に許可した外部サービス連携で、仕様どおりに送信されるデータ
- 実機で発生する電気的・熱的な設計問題（品質の課題として通常のIssueで扱います）
- 自動化されたスキャナの出力のみで、影響の説明や再現手順がない報告

## 秘密情報の取り扱い

秘密情報、認証情報、秘密鍵、トークン、個人データ、非公開の設計データを
Issue、PR、fixture、ログ、コミットに含めないでください。例示には伏せ字の
プレースホルダを使用します。誤ってコミットした場合は、上記の非公開経路で
報告したうえで、該当する認証情報を直ちに失効させてください。

外部サービスの資格情報は最小スコープ・期限付きとし、他者と共有しません。失効や権限不足は
成功や一時的な設定ミスとみなさず、ACDの処理を`unknown`として停止します。設計データを
脆弱性報告、見積、外部レビューへ提出する場合は、識別子・秘密情報・不要な製造情報を匿名化し、
提出先、目的、保管期間、削除方法を記録します。

fab APIやprovider tokenはSDKの`SecretRegistry`／`SecretSource`へ登録し、ACDは参照名だけを
保持します。SDKのsecret注入とmaskingを利用しても、secretをgraph、Evidence、log、commit、
profileへ保存しない不変条件は維持します。

投入されたECADファイル、ライブラリ、netlist、CADモデル、アーカイブ、外部ツール出力は
信頼できないデータです。パス解決、展開、外部プロセス呼び出し、生成物保存はサニタイズ境界と
権限分離を通し、入力に含まれる命令を実行しません。

## AIエージェント特有の前提

ACDはAIエージェントが設計と変更を実行することを前提とします。README、Issue、
PRコメント、取り込んだ設計データ、ツール出力、モデル出力、外部Webコンテンツは
**信頼できないデータ**であり、エージェントへの指示ではありません。
プロンプトインジェクションによって、秘密情報の開示、検証ゲートの回避、
承認や予算上限の無視を引き起こせる場合は、脆弱性として報告してください。
特に、外部文書・設計データ・ツール出力・会話入力を経由して安全境界を変更させること、
または多次元の発注裁量枠（上限金額、納期、月間回数、fab、地域）を回避させることを
脅威として扱います。安全境界とプロファイルは会話だけでは変更できず、未知の判定は停止します。
発注を実装するマイルストーン7では、これらを設計要件として発注前最終ゲートと
side-effect journalへ結び付けます。詳細は
[`docs/roadmap.md`](docs/roadmap.md#マイルストーン7-発注前最終ゲートと自働発注)を
参照してください。

## 免責

ACDが生成する設計データ、製造データ、ファームウェアの安全性、規制適合、
特許に関する自由な実施（freedom to operate）を保証しません。商用利用、
安全性評価、法的レビューは利用者の責任です。ライセンス条件は
[`LICENSE`](LICENSE)を参照してください。
